# 英语宝典 App — 技术选型文档

> 关联文档：[英语宝典_PRD_终稿.md](./英语宝典_PRD_终稿.md)

---

## 技术栈总览

| 层 | 选型 | 版本 | 说明 |
|---|---|---|---|
| **前端框架** | Flutter | 3.x (stable) | 跨平台 iOS/Android，自绘引擎 |
| **前端语言** | Dart | 3.x | — |
| **后端框架** | FastAPI | ≥0.110 | Python 异步 Web 框架 |
| **后端语言** | Python | ≥3.11 | — |
| **LLM 编排** | LangChain | ≥1.0 | LLM 调用链与工具集成 |
| **LLM Agent** | LangGraph | ≥1.0 | 多步骤 AI 工作流编排 |
| **大模型** | DeepSeek API | — | 语法分析、作文批改、AI 推荐 |
| **语音评测** | 讯飞云端语音评测 API | — | 音素级/单词级评分 |
| **OCR** | PaddleOCR-VL 在线接口 | — | 拍照识字、手写识别 |
| **关系数据库** | PostgreSQL | ≥15 | 主数据存储 |
| **缓存** | Redis | ≥7 | 会话、排行榜、AI 结果缓存 |
| **本地数据库** | SQLite (sqflite) | — | Flutter 端离线存储 |
| **ORM** | SQLAlchemy | ≥2.0 (async) | 异步数据库操作 |
| **对象存储** | MinIO / S3 兼容 | — | 语音录音、OCR 图片 |
| **容器化** | Docker + Docker Compose | — | 本地开发与部署 |
| **反向代理** | Nginx | — | 生产环境 |
| **包管理** | Poetry (Python) | — | 后端依赖管理 |

---

## 前端 (Flutter) 核心依赖

| 包名 | 用途 |
|---|---|
| `sqflite` | 本地 SQLite（离线单词本、SM-2 调度） |
| `riverpod` | 状态管理 |
| `go_router` | 路由导航 |
| `dio` | HTTP 客户端 |
| `flutter_tts` | 单词发音 TTS |
| `record` | 跟读录音 |
| `audio_waveforms` | 录音波形显示 |
| `fl_chart` | 学习数据图表 |
| `shared_preferences` | 本地配置/缓存 |
| `firebase_analytics` | 数据埋点（备选） |

---

## 后端 (FastAPI) 核心依赖

| 包名 | 用途 |
|---|---|
| `fastapi` | Web 框架 |
| `uvicorn[standard]` | ASGI 服务器 |
| `sqlalchemy[asyncio]` | 异步 ORM |
| `asyncpg` | PostgreSQL 异步驱动 |
| `alembic` | 数据库迁移 |
| `redis[hiredis]` | 异步 Redis 客户端 |
| `langchain≥1.0` | LLM 调用链 |
| `langgraph≥1.0` | AI Agent 工作流 |
| `langchain-deepseek` | DeepSeek 集成 |
| `pydantic≥2.0` | 数据校验 |
| `python-jose` | JWT 认证 |
| `httpx` | 异步 HTTP（调用讯飞/PaddleOCR） |
| `boto3` | S3 兼容对象存储 |

---

## LangChain / LangGraph 应用场景

遵循 PRD 中的 AI 需求，LangChain + LangGraph 用于以下场景：

### LangChain (`langchain≥1.0`)
- **LLM 调用封装**：统一管理 DeepSeek API 调用（语法分析、作文批改）
- **Prompt Template 管理**：预设系统提示词，确保输出结构稳定（JSON 格式的语法标注）
- **RAG 链路**：语法知识库 + 真题切片检索增强生成

### LangGraph (`langgraph≥1.0`)
- **AI 题库推荐工作流**：分析错题 → 定位薄弱语法点 → 检索题库 → 生成推荐
- **跨模块联动 Agent**：
  - 语法错题暴露的搭配错误 → 提升单词速记复习权重
  - 跟读读错单词 → 加入"顽固词"队列 → 触发额外拼写测试
- **作文批改多步骤流程**：OCR 提取 → 语法纠错 → 修改建议 → 生成讲解

---

## 项目结构（初步规划）

```
english-treasure/
├── frontend/                # Flutter App
│   └── lib/
│       ├── features/
│       │   ├── auth/        # 登录/注册
│       │   ├── vocab/       # 单词速记
│       │   ├── grammar/     # 语法分析
│       │   ├── speak/       # 跟读语感
│       │   └── profile/     # 个人中心
│       ├── core/            # 公共工具、主题、路由
│       └── main.dart
├── backend/                 # FastAPI
│   ├── app/
│   │   ├── api/             # 路由层
│   │   ├── agents/          # LangGraph 工作流
│   │   ├── chains/          # LangChain 调用链
│   │   ├── models/          # SQLAlchemy 数据模型
│   │   ├── schemas/         # Pydantic 请求/响应模型
│   │   ├── services/        # 业务逻辑
│   │   └── core/            # 配置、依赖注入
│   ├── alembic/             # 数据库迁移
│   └── docker-compose.yml
└── docs/
    ├── 英语宝典_PRD_终稿.md
    └── 技术选型.md
```
